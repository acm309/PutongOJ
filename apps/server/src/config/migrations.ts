/**
 * Legacy MongoDB runtime migrations have been retired. PostgreSQL schema migrations
 * are managed by Prisma and historical transforms belong to apps/cli.
 */
export async function runMigrations () {}
